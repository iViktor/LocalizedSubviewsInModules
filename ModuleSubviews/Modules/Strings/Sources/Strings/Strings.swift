import SwiftUI

public enum L {
    public static let stringsCatalog: String = "strings"
    public static let first: LocalizedStringKey = "first"
    public static let second: LocalizedStringKey = "second"
}
